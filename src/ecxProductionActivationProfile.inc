#error "replace with reviewed source-frozen network/deployment values before enabling ECX_PRODUCTION_ACTIVATION_PROFILE_FROZEN"

/* The generated replacement contains exactly one network and every required
 * base/V18 activation setting as ECX_PRODUCTION_NETWORK,
 * ECX_PRODUCTION_RELEASE_RECORD_SHA256, and ECX_PRODUCTION_ARG invocations.
 * Values are compiled into the executable; operator overrides are rejected. */
